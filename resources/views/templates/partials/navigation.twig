<nav class="navbar navbar-expand-lg navbar-dark bg-dark fixed-top">
 <div class="container">
   <a class="navbar-brand" href="{{ path_for('home') }}">Widgets Dashboard</a>
     <ul class="navbar-nav mr-auto">
       <li class="nav-item">
         <span class="nav-link">{% if user %} Hi {{ user.name }}!{% endif %} </span>
       </li>

     </ul>
     <ul class="navbar-nav">
       <li class="nav-item">
         {% if user %}
         <a class="nav-link" href="{{ path_for('signout') }}">Sign out</a>
         {% endif %}
       </li>
     </ul>
 </div>
</nav>
